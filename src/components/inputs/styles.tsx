import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    width: '100%',
    height: 45,
    paddingLeft: 10,
    borderRadius: 5,
    marginTop: 10,
    backgroundColor: '#fff',
    fontSize: 16,
    borderColor: 'black',
    borderWidth: 0.2,
  },
});

export default styles;
